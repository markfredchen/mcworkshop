production.mode=${production.mode}

mybatis.environment.id=test

JDBC.url=${JDBC.test.url}
JDBC.driver=${JDBC.test.driver}
JDBC.username=${JDBC.test.username}
JDBC.password=${JDBC.test.password}

log.dir=${test.log.dir}

report.dir=${test.report.dir}